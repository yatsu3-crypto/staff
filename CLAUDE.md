# clinic-ai — やまもと皮膚科・漢方クリニック 院内アプリ

## クリニック基本情報

- 名称：やまもと皮膚科・漢方クリニック
- 所在地：兵庫県姫路市・姫路駅前
- 診療：皮膚科と漢方・鍼灸を統合した診療
- 規模：1日 80〜100 人の外来
- 主な利用者：医師・看護師・受付スタッフ（院内向け）、および患者説明用の印刷物

このリポジトリは、診察室・処置室で iPhone を片手に使う説明補助ツールと、患者に渡すパンフレットを置く場所。

## 既存アプリ

- `index.html` — 「生活指導アシスタント」。8タブ構成（生活指導 / 当院製品 / 医師・看護師 / 外用薬 / ビタミン剤 / パッチ / 食物アレルギー / 所見別ナビ）。約 5,400 行の単一ファイル。パッチテスト画面と患者用パンフレットは `<script type="text/html" id="patch-src">` / `id="pamph-src"` に素の HTML として置き、読み込み時に iframe の `srcdoc` へ流し込んでいる（ブロック内で `</script>` を書くときは `<\/script>`）。
  - 全文を一度に読まない。`grep -n` で場所を特定してから `sed -n` で必要範囲だけ読む。
  - 既存の localStorage キー：`ptd_list`、`vit_fav_v1`、`vit_memo_v1`、`alg_avoid_state_v1`。
  - 新規アプリを作るときは index.html を改変しない（別ファイルにする）。
  - **データを直したら `DATA_META`（`var DATA_META = {` で検索）の該当タブの `updated` を今日の日付にする。** 各タブ末尾に更新日が表示され、12か月を超えると「更新なし」の注意が自動で出る。版を上げるときは `APP_VERSION`。
  - 同じ薬剤が複数タブに出るときの「用法・注意の正本」は `CANON_RULES` で定義している（当院製品 / 外用薬一覧 / 当院製剤 / 医師・看護師 / ビタミン剤）。薬剤や別名を追加したらここにも足す。
  - 横断検索（`gs-input`）は各タブのデータから索引を自動生成するので、データ追加時に検索側の変更は不要。

## 技術ルール（全アプリ共通）

1. **単一 HTML ファイル 1 つ**で完結する。CSS・JS・画像（base64）を全て内包し、ビルド工程・フレームワーク・npm を使わない。
2. **外部依存なし**。CDN の JS/CSS、外部 API、fetch は禁止。唯一の例外は Google Fonts の `<link>`（Shippori Mincho / Zen Kaku Gothic New）で、オフライン時はシステムフォントにフォールバックさせる。
3. **保存は localStorage** のみ。キー名は `<app>_<item>_v1` の形式（例：`patch_list_v1`）。`try/catch` で囲み、失効・無効時も動作させる。
4. **iPhone 表示優先**。390px 幅（iPhone 14/15）を基準に 375px（SE）〜430px（Pro Max）で崩れないこと。1カラム、タップ領域 44px 以上、入力欄フォント 16px 以上、`env(safe-area-inset-*)` 対応。
5. **GitHub Pages に直接置く**。ファイルはこのフォルダ直下に `<英小文字名>.html` として保存し、`https://…/<名前>.html` で開ける状態で納品する。サブフォルダやアセット分割はしない。
6. **見た目は `.claude/skills/washi-design/SKILL.md` に従う**。新規アプリは `.claude/agents/app-builder.md` の担当者に任せる。

## 患者個人情報の取り扱い（絶対禁止）

**患者個人情報を含むファイルは絶対に読み書きしない。**

- 氏名、患者ID、生年月日、住所、電話、保険情報、診療記録、写真、検査結果など、個人を特定できる情報を含むファイル（CSV、Excel、PDF、画像、電子カルテのエクスポート等）は、読み取り・作成・編集・コピー・要約のいずれも行わない。
- そのようなファイルが作業フォルダに見つかっても開かず、ユーザーにその旨を伝えて指示を待つ。
- アプリのサンプルデータ・テストデータに実在の患者情報を使わない。架空の値（「患者A」「テスト太郎」等）を使う。
- アプリ側でも患者個人情報を localStorage に保存する設計をしない。保存してよいのは、選択状態・お気に入り・メモなどスタッフ自身の作業状態のみ。

## 医療コンテンツについて

- 薬剤名・用量・適応・禁忌などの医学的内容はユーザー（医師）の指示・確認を前提とする。Claude が独自に医学的判断を追加・変更しない。
- 患者向け・スタッフ向けの説明文を書くときは、患者に見せる可能性を意識した丁寧語にし、免責文（「最終判断は医師が行います」）を入れる。

## 作業の進め方

- 新規アプリ：要件（名前・用途・使う人・保存するもの）を確認 → `app-builder` エージェントで作成 → iPhone 幅スクリーンショットで自己チェック → 単一 HTML を納品。
- 修正：該当ファイルを部分的に読んで最小限の差分で直す。デザイン変更は washi-design のトークンを守る。
- 表示確認は `.claude/skills/washi-design/scripts/iphone-shot.sh <html> 390 1200` を使う（Google Chrome ヘッドレス）。
